server:
  port: 8877

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/imoocbootscala?useSSL=false&serverTimezone=UTC
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
